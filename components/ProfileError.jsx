import { View, Text } from "react-native"

export default function ProfileError(){
    return(
        <View >
            <Text>Failed to load profile data. Please try again later.</Text>
        </View>
    )
}